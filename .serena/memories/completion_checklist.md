# Post-Task Checklist

Before considering a task complete, ensure:
1. All Go code is formatted using `go fmt`.
2. All backend tests pass: `go test ./...`.
3. If new dependencies were added, run `go mod tidy`.
4. The application builds without errors: `wails build`.
5. Frontend changes are verified in `wails dev`.
